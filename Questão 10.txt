#include <stdio.h>
#include <stdlib.h>
int main()
{
    int  dentro = 0, fora = 0, n;
    int i = 0;
    while(i < 10)
    {
        printf("Digite um numero: ");
        scanf("%d", &n);
        if(n>=10 && n<=50)
        {
            dentro++;
        }
        else
        {
            fora++;
        }
        i++;
    }
    printf("A quantidade de numeros dentro do intervalo entre 10 e 50 eh: %d \n", dentro);
    printf("A quantidade de numeros fora do intervalo entre 10 e 50 eh: %d \n", fora);
    return 0;
}